import Home from './pages/Home';

// Composant racine de l'application.
// Phase 0 : une seule page (l'accueil). Le routage entre plusieurs pages
// (connexion, tableau de bord, etc.) sera ajoute a la Phase 1.
export default function App() {
  return <Home />;
}
